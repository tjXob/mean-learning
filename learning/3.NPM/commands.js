npm install // to build the package.json configuration file
npm update // Updated the dependencies according to the corresoponding conf. file
npm start // Runs the start command set up in the start flag
npm install MODULE --save // Installs the MODULE and modifies the conf. file
npm install MODULE --save-dev// Installs the MODULE and modifies the conf. file dev dependencies.

// To install just production packages, when deployed in production
npm install --production
